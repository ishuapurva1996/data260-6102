# Part 1 review resolution

The full code review completed under run `20260920-013137-17f40697`; its original receipt is preserved in `code-review.json`. It covered correctness, testing, maintainability, security, API contracts, reliability, and a local adversarial pass. The original verdict was **Ready with fixes**. Both retained findings were then resolved; no retained finding remains open.

- **#5 — cookie timestamp boundary:** The browser login helper compares the first dashboard request with the cookie actually issued by POST /login. It retains the later browser-jar cookie for replay and attribute inspection. `timestamp-boundary.txt` records a real TestClient proof that Starlette re-signs a session across timestamps 1000 and 1001 without changing its payload. `cookie-helper-regression.cjs` extracts and executes the current collector helper against controlled response/request objects; its captured output proves acceptance when the later jar value differs and rejection when the sent value is unrelated. The final browser suite separately exercises actual HTTPS traffic. Synthetic helper fixtures are not represented as browser evidence.
- **#2 — OpenAPI contracts:** Route metadata now declares login/logout 303 responses, invalid-login 401 HTML, and the anonymous-dashboard 303 redirect. Five parameterized tests compare real responses with generated OpenAPI. The fixing agent observed four failures and one pass before the metadata change; that red result exists in the session tool transcript rather than a saved raw log. The final parent-run Python output is retained in `pytest-auth-api.txt` and independently confirms all five cases pass.

The browser launcher now explicitly requests one Uvicorn worker, matching the process-local session and rental stores. The vendored Bootstrap distribution includes its MIT license. These narrow finishing changes were checked before the code commit.

Final report and screenshot acceptance is performed by the parent session, outside the earlier code-only review scope. The delivered report uses the current auth.py source, final tested code commit, actual runtime counts, and separate records for manual visual inspection and automated verification.

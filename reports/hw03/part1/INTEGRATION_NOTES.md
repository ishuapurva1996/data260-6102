# Part 1 integration notes

- Worktree: `/Users/pragyaapurva/Documents/SJSU/DATA 260/HW3/worktrees/part1-auth`; branch: `codex/hw3-part1-auth`; common base: `3d5f99635c5b7129a2db7da23e075de48df94cb0`.
- Merge this branch in the later integration session while preserving Part 2's committed-question history. This session neither merged Part 2 nor changed its files.
- The owned changes extend the existing app in `code/web_application/`, root web `requirements.txt`, the two auth test files, Part 1 run/verifier scripts, and Part 1 report/raw/screenshot directories. Root README, .gitignore, package scripts, Dockerfile, previous reports and tags remain unchanged.
- The homepage now comes from Jinja `templates/index.html`; `static/index.html` was removed. Existing rental IDs, JavaScript and API behavior are preserved. `scripts/verify_hw02.py` asserts the old static-homepage bytes and is intentionally unchanged; reproduce that historical verifier on its historical tag. Current `tests/test_api.py` and `tests/browser_part2.cjs` are the preserved rental regressions. The latter filename refers to HW2 Part 2, not HW3 retrieval.
- Web dependencies are isolated in ignored `.venv-web`; Part 2 uses its own environment. Local certificates and private keys are ignored under `tmp/https`. No signing key, cookie token, certificate, tutor input, assignment PDF, environment, or plan is submission material.
- IPv4 `127.0.0.1:8702` was occupied by PID 50746 running from the shared checkout at inspection time. It was left untouched. Part 1 evidence uses the available IPv6 loopback address `[::1]:8702`, retaining PORT_BASE 8702. The browser suite checks the address before starting its own server and shuts down only its own child processes.
- Default idle timeout is 300 seconds, cookie lifetime 3600 seconds. The browser evidence also starts a separate 2-second demonstration process. That override is not a source/configuration default change. Automated browsers explicitly accept the self-signed development certificate; system trust is unchanged.
- One worker and memory-only storage are required. Restart revokes sessions and resets rental seeds. Demo credentials are public `admin / password`. The rental API remains public, as scoped; production authentication, CSRF hardening, persistent users, and multiworker session storage are outside this assignment demonstration.
- Direct main.py and Uvicorn imports are supported; the documented HTTPS path uses `scripts/run_hw03_web.py`. The Dockerfile still copies all runtime files from web_application; the web README includes an HTTPS command override mounting local TLS files. A Docker image was not built in this session.
- Later integration owns root README/package command updates, aggregate RUN_LOG.txt, AI_USE.md, verification.json, run instructions, the combined `reports/hw03/report.pdf`, external `Apurva_HW3.pdf`, repository/link/access checks, the final `hw3` tag, and any publishing or submission.
- Use `REPORT_SECTION.md` and its linked images together. It includes only full auth.py source at the bottom. Merge the Part 1 AI disclosure with Part 2's disclosure, preserving the honest distinction between automated work and student review.
- The Part 1 verifier checks source hashes against the code commit and current files. After integration changes source or package files, rerun the browser evidence and verifier on the integrated code commit; do not reuse old passing evidence as proof of new code.

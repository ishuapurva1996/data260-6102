# Homework 2 submission evidence

The application is extended in the repository's shared `code/` and `src/` folders. Its current frontend is in `code/web_application/static/`; Part 2 will add the FastAPI backend to the same application.

This directory is reserved for the assignment-required `RUN_LOG.txt`, `METRICS.md`, `AI_USE.md`, `report.pdf`, `verification.json`, raw evidence, and reproducible run instructions. Those deliverables are not complete yet. For each numbered question, place the relevant code excerpt immediately above its corresponding output screenshot in the final report; input screenshots are supplementary.

Part 1 is complete. Part 2 is planned and has not been implemented. The implementation plan and detailed screenshot guide remain outside Git in `../../../HW2/agent_outputs/`, relative to this directory. Only actual submission evidence belongs here.
